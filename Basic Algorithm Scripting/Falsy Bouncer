function fill(value){
  if (value===false){
    return value;
  }else{
    return value;
  }
}


function bouncer(arr) {
  // Don't show a false ID to this bouncer.
  return arr.filter(fill);
}

bouncer([7, "ate", "", false, 9]);
